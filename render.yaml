services:
  - type: web
    name: hudson-float-plan
    runtime: python
    plan: free
    region: ohio
    buildCommand: "python build.py"
    startCommand: "python server.py"
    healthCheckPath: /api/health
    autoDeploy: true
